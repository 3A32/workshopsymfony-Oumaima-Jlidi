{% extends 'base.html.twig' %}
{% block titlepage %}Ajout student{% endblock %}
{% block body %}
 <div class="card-header header-elements-inline" style="background-color:Teal">
						<h3 class="card-title" style="color:white">Ajout nouveau student</h3>
                         <a class="btn btn-dark" style="width:150px;margin-left: 900px;" href="{{ path('app_club_student') }}"> <i class="icon-reply"></i>Liste des students</a></div>
<br>
{{ form_start(form) }}
    {{ form_widget(form) }}
        <button  class="btn btn-success" style="width:100px;margin-left: 90%;position: relative;"  > <i class="icon-floppy-disk"></i> {{ button_label|default('Valider') }}</button>
{{ form_end(form) }}

</div>

{%endblock%}